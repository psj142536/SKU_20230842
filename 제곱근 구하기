#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
int main(void)
{
	float a = 0;
	while (1)
	{
		printf("실수값을 입력하시오: ");
		scanf("%f", &a);
		if (a < 0)
			break;
		printf("%f의 제곱근은 %f입니다.\n", a, sqrt(a));
	}
	return 0;
}
